// Code generated by wacogo-witgen from wit. DO NOT EDIT.

// Package logging provides Go bindings for the wasi:logging/logging interface.
package logging

import (
	"context"
	"fmt"

	"github.com/partite-ai/wacogo"
	"github.com/partite-ai/wacogo/host"
)

// loggingWrapper is the concrete type returned by WrapInstance
// as a Logging. Held internally; users hold it via the Logging
// interface.
type loggingWrapper struct {
	callerInst *host.ComponentInstance
	fnLog      *host.ExportedFunc
}

// WrapInstance returns a Logging that forwards method calls to
// callee. caller is a Factory-built host instance whose state hosts any
// handles minted by callee during those calls.
func WrapInstance(caller *host.ComponentInstance, callee *wacogo.ComponentInstance) Logging {
	w_ := &loggingWrapper{callerInst: caller}
	w_.fnLog = host.WrapExportedFunc(callee.ExportedFunc("log"))
	return w_
}
func (w *loggingWrapper) Log(ctx context.Context, level Level, context string, message string) error {
	var resErr_ error
	arg0_ := level
	arg1_ := context
	arg2_ := message
	{
		callErr_ := w.fnLog.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
				stack[0] = uint64(arg0_)
				if err_ := lowerFlatString(ctx, caller, callee, w.callerInst, w.fnLog.ParamType(1), stack[1:3], arg1_); err_ != nil {
					resErr_ = err_
					return
				}
				if err_ := lowerFlatString(ctx, caller, callee, w.callerInst, w.fnLog.ParamType(2), stack[3:5], arg2_); err_ != nil {
					resErr_ = err_
					return
				}
			},
			func(caller, callee *host.CallContext, stack []uint64) {
			},
		)
		if callErr_ != nil {
			return fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
		if resErr_ != nil {
			return resErr_
		}
	}
	return nil
}
