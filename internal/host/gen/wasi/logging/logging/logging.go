// Code generated by wacogo-witgen from wit. DO NOT EDIT.

// Package logging provides Go bindings for the wasi:logging/logging interface.
package logging

import (
	"context"
)

// InterfaceName is the canonical WIT id of the interface this binding
// was generated from.
const InterfaceName = "wasi:logging/logging"

// Logging is the Go binding for the wasi:logging/logging interface.
// Implement it to provide a host-side component, or pass an exported wasm
// instance to WrapInstance to obtain a Logging that calls into wasm.
// Resource-typed parameters and results appear as *<R>Handle values that can
// be passed across component boundaries.
type Logging interface {
	Log(ctx context.Context, level Level, context string, message string) error
}

// Level corresponds to the WIT enum level.
type Level uint8

const (
	LevelTrace    Level = 0
	LevelDebug    Level = 1
	LevelInfo     Level = 2
	LevelWarn     Level = 3
	LevelError    Level = 4
	LevelCritical Level = 5
)

func (v Level) String() string {
	switch v {
	case LevelTrace:
		return "trace"
	case LevelDebug:
		return "debug"
	case LevelInfo:
		return "info"
	case LevelWarn:
		return "warn"
	case LevelError:
		return "error"
	case LevelCritical:
		return "critical"
	}
	return "Level(unknown)"
}
