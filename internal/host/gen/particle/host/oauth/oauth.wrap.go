// Code generated by wacogo-witgen from wit. DO NOT EDIT.

// Package oauth provides Go bindings for the particle:host/oauth interface.
package oauth

import (
	"context"
	"fmt"

	"github.com/partite-ai/wacogo"
	"github.com/partite-ai/wacogo/host"
)

// oauthWrapper is the concrete type returned by WrapInstance
// as a Oauth. Held internally; users hold it via the Oauth
// interface.
type oauthWrapper struct {
	callerInst *host.ComponentInstance
	fnRefresh  *host.ExportedFunc
}

// WrapInstance returns a Oauth that forwards method calls to
// callee. caller is a Factory-built host instance whose state hosts any
// handles minted by callee during those calls.
func WrapInstance(caller *host.ComponentInstance, callee *wacogo.ComponentInstance) Oauth {
	w_ := &oauthWrapper{callerInst: caller}
	w_.fnRefresh = host.WrapExportedFunc(callee.ExportedFunc("refresh"))
	return w_
}
func (w *oauthWrapper) Refresh(ctx context.Context, name string) (Result_OauthError, error) {
	var resVal_ Result_OauthError
	var resErr_ error
	arg0_ := name
	{
		callErr_ := w.fnRefresh.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
				if err_ := lowerFlatString(ctx, caller, callee, w.callerInst, w.fnRefresh.ParamType(0), stack[0:2], arg0_); err_ != nil {
					resErr_ = err_
					return
				}
			},
			func(caller, callee *host.CallContext, stack []uint64) {
				if v_, err_ := liftMemResult_OauthError(ctx, caller, callee, w.callerInst, w.fnRefresh.ResultType(0), uint32(stack[0])); err_ != nil {
					resErr_ = err_
					return
				} else {
					resVal_ = v_
				}
			},
		)
		if callErr_ != nil {
			return nil, fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
		if resErr_ != nil {
			return nil, resErr_
		}
	}
	return resVal_, nil
}
