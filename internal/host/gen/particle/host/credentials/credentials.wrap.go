// Code generated by wacogo-witgen from wit. DO NOT EDIT.

// Package credentials provides Go bindings for the particle:host/credentials interface.
package credentials

import (
	"context"
	"fmt"

	"github.com/partite-ai/wacogo"
	"github.com/partite-ai/wacogo/host"
)

// credentialsWrapper is the concrete type returned by WrapInstance
// as a Credentials. Held internally; users hold it via the Credentials
// interface.
type credentialsWrapper struct {
	callerInst            *host.ComponentInstance
	fnGetConfiguredMethod *host.ExportedFunc
	fnGetPlaceholder      *host.ExportedFunc
	fnGetRaw              *host.ExportedFunc
}

// WrapInstance returns a Credentials that forwards method calls to
// callee. caller is a Factory-built host instance whose state hosts any
// handles minted by callee during those calls.
func WrapInstance(caller *host.ComponentInstance, callee *wacogo.ComponentInstance) Credentials {
	w_ := &credentialsWrapper{callerInst: caller}
	w_.fnGetConfiguredMethod = host.WrapExportedFunc(callee.ExportedFunc("get-configured-method"))
	w_.fnGetPlaceholder = host.WrapExportedFunc(callee.ExportedFunc("get-placeholder"))
	w_.fnGetRaw = host.WrapExportedFunc(callee.ExportedFunc("get-raw"))
	return w_
}
func (w *credentialsWrapper) GetConfiguredMethod(ctx context.Context, name string) (ResultOptionStringCredentialError, error) {
	var resVal_ ResultOptionStringCredentialError
	var resErr_ error
	arg0_ := name
	{
		callErr_ := w.fnGetConfiguredMethod.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
				if err_ := lowerFlatString(ctx, caller, callee, w.callerInst, w.fnGetConfiguredMethod.ParamType(0), stack[0:2], arg0_); err_ != nil {
					resErr_ = err_
					return
				}
			},
			func(caller, callee *host.CallContext, stack []uint64) {
				if v_, err_ := liftMemResultOptionStringCredentialError(ctx, caller, callee, w.callerInst, w.fnGetConfiguredMethod.ResultType(0), uint32(stack[0])); err_ != nil {
					resErr_ = err_
					return
				} else {
					resVal_ = v_
				}
			},
		)
		if callErr_ != nil {
			return nil, fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
		if resErr_ != nil {
			return nil, resErr_
		}
	}
	return resVal_, nil
}

func (w *credentialsWrapper) GetPlaceholder(ctx context.Context, name string) (ResultPlaceholderInfoCredentialError, error) {
	var resVal_ ResultPlaceholderInfoCredentialError
	var resErr_ error
	arg0_ := name
	{
		callErr_ := w.fnGetPlaceholder.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
				if err_ := lowerFlatString(ctx, caller, callee, w.callerInst, w.fnGetPlaceholder.ParamType(0), stack[0:2], arg0_); err_ != nil {
					resErr_ = err_
					return
				}
			},
			func(caller, callee *host.CallContext, stack []uint64) {
				if v_, err_ := liftMemResultPlaceholderInfoCredentialError(ctx, caller, callee, w.callerInst, w.fnGetPlaceholder.ResultType(0), uint32(stack[0])); err_ != nil {
					resErr_ = err_
					return
				} else {
					resVal_ = v_
				}
			},
		)
		if callErr_ != nil {
			return nil, fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
		if resErr_ != nil {
			return nil, resErr_
		}
	}
	return resVal_, nil
}

func (w *credentialsWrapper) GetRaw(ctx context.Context, name string) (ResultStringCredentialError, error) {
	var resVal_ ResultStringCredentialError
	var resErr_ error
	arg0_ := name
	{
		callErr_ := w.fnGetRaw.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
				if err_ := lowerFlatString(ctx, caller, callee, w.callerInst, w.fnGetRaw.ParamType(0), stack[0:2], arg0_); err_ != nil {
					resErr_ = err_
					return
				}
			},
			func(caller, callee *host.CallContext, stack []uint64) {
				if v_, err_ := liftMemResultStringCredentialError(ctx, caller, callee, w.callerInst, w.fnGetRaw.ResultType(0), uint32(stack[0])); err_ != nil {
					resErr_ = err_
					return
				} else {
					resVal_ = v_
				}
			},
		)
		if callErr_ != nil {
			return nil, fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
		if resErr_ != nil {
			return nil, resErr_
		}
	}
	return resVal_, nil
}
