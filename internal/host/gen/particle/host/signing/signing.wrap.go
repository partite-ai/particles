// Code generated by wacogo-witgen from wit. DO NOT EDIT.

// Package signing provides Go bindings for the particle:host/signing interface.
package signing

import (
	"context"
	"fmt"

	"github.com/partite-ai/wacogo"
	"github.com/partite-ai/wacogo/host"
)

// signingWrapper is the concrete type returned by WrapInstance
// as a Signing. Held internally; users hold it via the Signing
// interface.
type signingWrapper struct {
	callerInst *host.ComponentInstance
	fnSign     *host.ExportedFunc
	fnVerify   *host.ExportedFunc
}

// WrapInstance returns a Signing that forwards method calls to
// callee. caller is a Factory-built host instance whose state hosts any
// handles minted by callee during those calls.
func WrapInstance(caller *host.ComponentInstance, callee *wacogo.ComponentInstance) Signing {
	w_ := &signingWrapper{callerInst: caller}
	w_.fnSign = host.WrapExportedFunc(callee.ExportedFunc("sign"))
	w_.fnVerify = host.WrapExportedFunc(callee.ExportedFunc("verify"))
	return w_
}
func (w *signingWrapper) Sign(ctx context.Context, name string, data []uint8) (ResultListU8SigningError, error) {
	var resVal_ ResultListU8SigningError
	var resErr_ error
	arg0_ := name
	arg1_ := data
	{
		callErr_ := w.fnSign.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
				if err_ := lowerFlatString(ctx, caller, callee, w.callerInst, w.fnSign.ParamType(0), stack[0:2], arg0_); err_ != nil {
					resErr_ = err_
					return
				}
				if err_ := lowerFlatListU8(ctx, caller, callee, w.callerInst, w.fnSign.ParamType(1), stack[2:4], arg1_); err_ != nil {
					resErr_ = err_
					return
				}
			},
			func(caller, callee *host.CallContext, stack []uint64) {
				if v_, err_ := liftMemResultListU8SigningError(ctx, caller, callee, w.callerInst, w.fnSign.ResultType(0), uint32(stack[0])); err_ != nil {
					resErr_ = err_
					return
				} else {
					resVal_ = v_
				}
			},
		)
		if callErr_ != nil {
			return nil, fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
		if resErr_ != nil {
			return nil, resErr_
		}
	}
	return resVal_, nil
}

func (w *signingWrapper) Verify(ctx context.Context, name string, data []uint8, signature []uint8) (ResultBoolSigningError, error) {
	var resVal_ ResultBoolSigningError
	var resErr_ error
	arg0_ := name
	arg1_ := data
	arg2_ := signature
	{
		callErr_ := w.fnVerify.CallRaw(ctx, w.callerInst,
			func(caller, callee *host.CallContext, stack []uint64) {
				if err_ := lowerFlatString(ctx, caller, callee, w.callerInst, w.fnVerify.ParamType(0), stack[0:2], arg0_); err_ != nil {
					resErr_ = err_
					return
				}
				if err_ := lowerFlatListU8(ctx, caller, callee, w.callerInst, w.fnVerify.ParamType(1), stack[2:4], arg1_); err_ != nil {
					resErr_ = err_
					return
				}
				if err_ := lowerFlatListU8(ctx, caller, callee, w.callerInst, w.fnVerify.ParamType(2), stack[4:6], arg2_); err_ != nil {
					resErr_ = err_
					return
				}
			},
			func(caller, callee *host.CallContext, stack []uint64) {
				if v_, err_ := liftMemResultBoolSigningError(ctx, caller, callee, w.callerInst, w.fnVerify.ResultType(0), uint32(stack[0])); err_ != nil {
					resErr_ = err_
					return
				} else {
					resVal_ = v_
				}
			},
		)
		if callErr_ != nil {
			return nil, fmt.Errorf("wacogo/witgen: CallRaw failed: %w", callErr_)
		}
		if resErr_ != nil {
			return nil, resErr_
		}
	}
	return resVal_, nil
}
