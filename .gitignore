.claude
notes.txt

# Build artifacts
target/
dist/
__pycache__/

/particle

# Per-component wit/ staging trees — the Makefile assembles each from
# the shared wit/ root before invoking cargo / wit_bindgen, so treating
# them as derived keeps the repo from drifting.
components/dyld-libdl/wit/deps/
components/python-runtime/wit/
components/wasm-example/wit/
