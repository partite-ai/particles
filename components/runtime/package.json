{
  "name": "@particle/runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "QuickJS-hosted particle runtime — TS source for the wasm-rquickjs wrapper.",
  "comments": [
    "No deps: this component bundles only its own TypeScript source — there are",
    "no library packages baked into the artifact. esbuild + tsc are host build",
    "tools (provided by the dev container, see .devcontainer/Dockerfile)."
  ]
}
